name: Features
description: 'Some'
branding:
  icon: 'chevrons-up'  
  color: 'green'

runs:
  using: "composite"
  steps:
    - name: GET ENV
      shell: bash
      run: |
        echo "TESTBOX_BRANCH_PLATFORM=${{ env.TESTBOX_BRANCH_PLATFORM }}" 
        echo "ANVIL_NUMBER=${{ env.ANVIL_NUMBER }}"
    - name: Check Always
      shell: bash
      if: always()
      run: |
        sleep 10
        echo -e "\e[32mGreen text Green text Green text Green text Green text"
        echo -e "\e[36mCIAN text CIAN text CIAN text CIAN text CIAN text CIAN text CIAN text "
        echo -e "\e[42mGreen  background Green  background Green  background Green  background "
        echo -e "\e[46mCIAN  background CIAN  background CIAN  background CIAN  background "

