name: Features
description: 'Some'
branding:
  icon: 'chevrons-up'  
  color: 'green'

runs:
  using: "composite"
  steps:
    - name: GET ENV
      shell: bash
      run: |
        echo "TESTBOX_BRANCH_PLATFORM=${{ env.TESTBOX_BRANCH_PLATFORM }}" 
        echo "ANVIL_NUMBER=${{ env.ANVIL_NUMBER }}"
    - name: Check Always
      shell: bash
      if: always()
      run: |
        sleep 10
        echo " **************  This Always from action"
        logs_link="sssssssssssssssssssssssssssssssssssssssssss"
        ::warning::Google Logs: ${logs_link}
