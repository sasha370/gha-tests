name: Features
description: 'Some'
branding:
  icon: 'chevrons-up'  
  color: 'green'

runs:
  using: "composite"
  steps:
    - name: GET ENV
      shell: bash
      run: |
        echo "TESTBOX_BRANCH_PLATFORM=${{ env.TESTBOX_BRANCH_PLATFORM }}" 
        echo "ANVIL_NUMBER=${{ env.ANVIL_NUMBER }}"
    - name: Check Always
      shell: bash
      if: always()
      run: |
        link="https://console.cloud.google.com/logs/quer"
        echo -e "\e[41mGreen text Green ${link}"
        echo -e "\e[42mGreen text Green ${link}"
        echo -e "\e[43mGreen text Green ${link}"
        echo -e "\e[44mGreen text Green ${link}"
        echo -e "\e[45mGreen text Green ${link}"
        echo -e "\e[46mGreen text Green ${link}"
        echo -e "\e[47mGreen text Green ${link}"
        echo -e "\e[100mGreen text Green ${link}"
        echo -e "\e[101mGreen text Green ${link}"
        echo -e "\e[102mGreen text Green ${link}"
        echo -e "\e[104mGreen text Green ${link}"
        echo -e "\e[106mGreen text Green ${link}"
        
        

